apiVersion: v1
kind: Secret
metadata:
  name: devops-demo-secret
  namespace: devops-demo
stringData:
  # replace with real values or use sealed-secrets / external vault
  DATABASE_URL: "postgres://user:password@db:5432/demo"
